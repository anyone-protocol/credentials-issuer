node_modules/
dist/
.env
*.log
.claude

# Development epoch keys. Stage and live get theirs from Vault.
# The whole directory, not a list of filenames: an earlier list named
# current.json, M1.2 renamed that to keyring.json, and the dev signing key was
# committed for four commits as a result.
config/keys/
