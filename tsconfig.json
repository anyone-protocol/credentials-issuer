{
  // Bun reads the tsconfig.json at the process cwd, so these must be duplicated
  // here for commands run from the repository root.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true
  }
}
